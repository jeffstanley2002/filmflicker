# Extra dependencies needed ONLY to run scripts/train_models.py locally.
# The two-tower taste-embedding model trains and serves with NumPy, so this
# file currently tracks the deploy requirements without adding TensorFlow.
-r requirements.txt
